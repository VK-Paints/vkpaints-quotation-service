apiVersion: v1
kind: Service
metadata:
  name: quotation-service
  namespace: vkpaints-dev-ns
spec:
  selector:
    app: quotation-service
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
